Durable memory for the [[Orchestrator]]: decisions, outcomes, escalations, and [[Transform Record]] provenance.

Receives write-back on every acceptance so the system learns and never re-hits a resolved wall blindly. Persists under the port repo `.claude/`.
