Whole-assembly verification (full `build`, all GameTests) run by [[AST_LSP]] after the per-unit [[Work Order]]s pass — because a unit passing is not the assembly passing.

Clears integrated output for the [[Orchestrator]] to deliver.
